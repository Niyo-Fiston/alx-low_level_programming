#include "main.h"

/**
 * _abs - function to return absolute value
 * @c: int for argument for function
 * Return: number
 */

int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
	{
		return (-c);
	}
}
